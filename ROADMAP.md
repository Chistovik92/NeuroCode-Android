# Дорожная карта NeuroCode Android

Статусы: ✅ готово · 🚧 в работе · ⬜ запланировано

## Пройдено

### 0.1.0 — базовая версия ✅

- [x] Чат с OpenAI-compatible провайдерами (OpenAI, OpenRouter, DeepSeek, Groq, Mistral + свои endpoint);
- [x] Агентный цикл: list_files, read_file, write_file, search_text, run_command, git_status, git_diff;
- [x] Подтверждение записи файлов и опасных shell-команд (ApprovalGate);
- [x] Проекты-песочницы, импорт папки через SAF;
- [x] Редактор Sora Editor, терминал /system/bin/sh;
- [x] Git через JGit: init, status, diff, stage, commit, история;
- [x] Локальный GGUF-inference (llama.cpp binding), шифрование ключей в Keystore;
- [x] CI: юнит-тесты + debug APK в GitHub Actions.

### 0.1.1 — экспорт проекта ✅

- [x] Копирование проекта в любую папку устройства через SAF с прогрессом,
  merge/overwrite, пропуск служебной `.neurocode/` и временных файлов.

### 0.1.2 — Git remote (HTTPS) ✅

- [x] `clone`, `pull`, `push` через JGit; токен — в Keystore (`SecureSecretStore`);
- [x] Карточка Remote и диалог клонирования в GitScreen; клонирование без локального `git init`;
- [x] Только HTTPS-адреса remote.

### 0.1.3 — надёжность агента ✅

- [x] SSE-стриминг облачных моделей с автооткатом на обычный запрос;
- [x] Память агента: результаты инструментов в истории диалога + сводки модели между запусками;
- [x] Тестируемые `CommandPolicy`/`PathGuard`, 15 юнит-тестов; усилен regex обхода каталогов;
- [x] Инструменты `replace_in_file` (уникальное вхождение) и `delete_file` (с копией в history);
- [x] Удалены неиспользуемые зависимости (navigation.compose, datastore, material).

## 0.2.0 — инженерное качество ✅

- [x] Release-подпись: локально через `keystore.properties`, в CI через секреты
  (`RELEASE_KEYSTORE_BASE64/RELEASE_STORE_PASSWORD/RELEASE_KEY_ALIAS/RELEASE_KEY_PASSWORD`),
  артефакт neurocode-release-apk; починен CI (sdkmanager path, pipefail, exec-биты, debug.keystore);
- [x] `AppViewModel` разделён на экранные ViewModel: Chat / Editor / Git / Terminal /
  Projects / Settings + общий UiMessageBus; AppViewModel стал корнем композиции;
- [x] Статический анализ detekt в CI с baseline (новые проблемы ломают сборку);
- [x] `versionName` берётся из файла `VERSION` автоматически при сборке;
- [x] Smoke-тест androidTest (запуск приложения, переход на «Настройки»)
  + job `instrumented` с эмулятором в GitHub Actions.

## 0.3.0 — файлы и синхронизация ⬜

- [ ] Двусторонняя синхронизация с внешней папкой: запоминание tree URI, сравнение
  по времени изменения и размеру, явный выбор направления при конфликте;
- [ ] Экспорт всего проекта одним ZIP-архивом;
- [ ] Переименование и перемещение файлов/папок из проводника редактора;
- [ ] Список недавних файлов с быстрым открытием.

## 0.4.0 — возможности агента ⬜

- [ ] Автоматический контекст проекта: сводка дерева и заголовков ключевых файлов
  в системный промпт (меньше лишних вызовов list_files/read_file);
- [ ] Пост-проверки: настраиваемые команды (lint/test), которые агент обязан прогнать
  после правок и учесть результат до ответа;
- [ ] Фолбэк провайдера: автоматический повтор задачи у резервного API при ошибке;
- [ ] Внешние инструменты: декларирование дополнительных инструментов агента в JSON.

## 0.5.0 — среда выполнения ⬜

- [ ] Модуль proot с базовым Linux-окружением для терминала и команд агента;
- [ ] Языковая подсветка / LSP в редакторе;
- [ ] Локализация интерфейса (strings.xml, en).

## Принципы версионирования

Номер версии ведётся в `VERSION` и `app/build.gradle.kts` (`versionName`/`versionCode`).
Версия помечается готовой, когда закрыты все её пункты и сборка зелёная.
Патч-версии (например 0.2.1) — только исправления без новых пунктов карты.

