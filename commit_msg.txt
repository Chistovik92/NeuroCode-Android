feat: 0.4.0 — post-checks, fallback provider, skills import/export, UI fixes

- ChatScreen: messenger-style layout, input bar fixed at bottom with imePadding, removed duplicate model chip
- NeuroCodeApp: clickable model badge in topbar opens ModelSwitcherDialog
- Models: added postChecksEnabled, postCheckCommands, fallbackProviderId
- ChatViewModel: post-checks execution after agent runs, fallback provider retry
- SettingsViewModel: setters + skills export/import via SAF
- SettingsScreen: UI for post-checks, fallback provider dropdown, skills import/export
- AndroidManifest: windowSoftInputMode=adjustResize for proper keyboard handling
- ROADMAP: 0.4 items checked, added GGUF download to 1.0
- VERSION: 0.4.0